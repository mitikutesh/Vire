/**
 * Scaffold placeholder — E0.5 replaces this with the real app shell
 * (four tabs over fixture data). Colors are inline until the design tokens
 * land in E0.3.
 */
export default function App() {
  return (
    <div
      className="flex min-h-screen items-center justify-center"
      style={{ background: '#F1F2ED' }}
    >
      <p className="text-xl font-extrabold" style={{ color: '#DD8F1F' }}>
        Vire
      </p>
    </div>
  );
}
