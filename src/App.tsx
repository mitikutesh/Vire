/**
 * Scaffold placeholder — E0.5 replaces this with the real app shell
 * (four tabs over fixture data).
 */
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-paper">
      <p className="disp text-cloud text-xl font-extrabold">Vire</p>
    </div>
  );
}
